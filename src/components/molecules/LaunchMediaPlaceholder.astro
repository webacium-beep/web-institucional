---
interface Props {
  fallbackText?: string;
}

const { fallbackText } = Astro.props;
---

<div class="h-full w-full md:w-1/2 flex-1 md:flex-none bg-gray-200">
  {fallbackText && <span>{fallbackText}</span>}
  <slot />
</div>
